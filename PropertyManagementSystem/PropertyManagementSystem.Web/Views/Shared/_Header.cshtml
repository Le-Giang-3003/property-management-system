<style>
    /* Header Background */
    .navbar-header-custom {
        /* background: linear-gradient(135deg, #3b82f6 0%, #1e40af 100%); */
        background: #2C6FFF;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
        padding: 10px 0;
    }


    .menu-toggle-circle {
        width: 40px;
        height: 40px;
        border-radius: 50%;
        border: none;
        background: #ffffff;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        gap: 4px;
        margin-right: 12px;
        cursor: pointer;
        box-shadow: 0 2px 6px rgba(0, 0, 0, 0.15);
        padding: 0;
    }

        .menu-toggle-circle span {
            width: 18px;
            height: 2px;
            background: #222;
            border-radius: 999px;
        }

    /* Logo/Brand */
    .navbar-brand-custom {
        font-size: 28px;
        font-weight: 700;
        color: white !important;
        margin-right: 40px;
        flex-shrink: 0;
    }

    /* Search Box - White Rounded */
    .search-wrapper {
        background: white;
        border-radius: 25px;
        padding: 8px 12px;
        box-shadow: 0 2px 6px rgba(0, 0, 0, 0.08);
        display: flex;
        align-items: center;
        gap: 6px;
        flex: 1;
        max-width: 650px; /* rộng hơn để chứa đủ 3 phần */
    }

        .search-wrapper input {
            border: none !important;
            background: transparent;
            font-size: 14px;
            color: #333;
            flex: 1;
            padding: 0;
        }

            .search-wrapper input::placeholder {
                color: #999;
            }

            .search-wrapper input:focus {
                outline: none;
                box-shadow: none !important;
            }

    .search-icon {
        color: #999;
        font-size: 16px;
    }

    /* Location Select */
    .location-select {
        background: white;
        border: 1px solid #e0e0e0;
        color: #666;
        padding: 8px 12px;
        border-radius: 4px;
        font-size: 12px;
        cursor: pointer;
    }

        .location-select:focus {
            outline: none;
            border-color: #3b82f6;
        }

    /* Search Button - Orange */
    .btn-search {
        background: #ff6b35 !important;
        border: none;
        color: white;
        padding: 8px 24px;
        border-radius: 20px;
        font-size: 13px;
        font-weight: 600;
        cursor: pointer;
        transition: all 0.3s ease;
    }

    .search-input {
        flex: 1;
        border: none !important;
        background: transparent;
        font-size: 14px;
        color: #333;
    }

        .search-input:focus {
            outline: none;
            box-shadow: none !important;
        }

    .location-select.inside {
        border-radius: 16px;
        padding: 6px 12px;
        font-size: 12px;
        border: 1px solid #e0e0e0;
        background: #fff;
        white-space: nowrap;
    }

    .btn-search.inside {
        padding: 6px 18px;
        border-radius: 16px;
        font-size: 13px;
        white-space: nowrap;
    }


    .btn-search:hover {
        background: #e55a24 !important;
        transform: translateY(-1px);
        box-shadow: 0 4px 12px rgba(255, 107, 53, 0.3);
    }

    /* Icon Buttons */
    .icon-btn {
        background: transparent;
        border: none;
        color: white;
        font-size: 20px;
        cursor: pointer;
        width: 40px;
        height: 40px;
        display: flex;
        align-items: center;
        justify-content: center;
        border-radius: 8px;
        transition: background 0.3s ease;
        position: relative;
    }

        .icon-btn:hover {
            background: rgba(255, 255, 255, 0.15);
        }

    /* Badge on Chat Icon */
    .badge-notification {
        position: absolute;
        top: 0;
        right: 0;
        background: #ff6b35;
        color: white;
        border-radius: 50%;
        width: 20px;
        height: 20px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 10px;
        font-weight: bold;
        border: 2px solid #1e40af;
    }

    /* Auth Buttons */
    .btn-action {
        border: none;
        padding: 10px 28px;
        border-radius: 20px;
        font-size: 13px;
        font-weight: 600;
        cursor: pointer;
        transition: all 0.3s ease;
        margin-left: 8px;
    }

    .btn-post {
        background: #1a1a1a;
        color: white;
    }

        .btn-post:hover {
            background: #000;
            transform: translateY(-1px);
        }

    .btn-login {
        background: white;
        color: #3b82f6;
    }

        .btn-login:hover {
            background: #f5f5f5;
            transform: translateY(-1px);
        }

    /* Responsive - Tablet */
    @@media (max-width: 1024px) {
        .navbar-brand-custom {
            margin-right: 20px;
        }

        .search-wrapper {
            max-width: 100%;
            margin-bottom: 12px;
        }
    }

    /* Responsive - Mobile */
    @@media (max-width: 768px) {
        .navbar-brand-custom {
            font-size: 24px;
            margin-right: 12px;
        }

        .btn-login {
            display: none;
        }

        .icon-btn {
            width: 36px;
            height: 36px;
            font-size: 18px;
        }
    }

    .category-panel {
        position: absolute;
        top: 70px; /* chỉnh cho khớp chiều cao header */
        left: 16px;
        width: 280px;
        background: #ffffff;
        border-radius: 16px;
        overflow: hidden;
        z-index: 1050;
        display: none; /* mặc định ẩn */
    }

    .category-header {
        padding: 12px 16px;
        font-weight: 600;
        border-bottom: 1px solid #f0f0f0;
        background: #ffffff;
    }

    .category-panel .list-group-item {
        font-size: 14px;
        border-left: none;
        border-right: none;
        border-color: #f0f0f0;
    }

        .category-panel .list-group-item span:first-child {
            width: 20px;
            text-align: center;
        }

</style>

<!-- Header Navigation -->
<nav class="navbar navbar-expand-lg navbar-header-custom sticky-top">
    <div class="container-fluid px-3 px-lg-4">

        <button class="menu-toggle-circle d-flex">
            <span></span>
            <span></span>
            <span></span>
        </button>

        <!-- Categories panel -->
        <div id="categoryPanel" class="category-panel shadow">
            <div class="category-header">
                Categories
            </div>
            <div class="list-group list-group-flush">
                <a href="#" class="list-group-item list-group-item-action d-flex align-items-center gap-2">
                    <span>💰</span> <span>Buy & Sell</span>
                </a>
                <a href="#" class="list-group-item list-group-item-action d-flex align-items-center gap-2">
                    <span>🏠</span> <span>For Rent</span>
                </a>
                <a href="#" class="list-group-item list-group-item-action d-flex align-items-center gap-2">
                    <span>🏗️</span> <span>Projects</span>
                </a>
                <a href="#" class="list-group-item list-group-item-action d-flex align-items-center gap-2">
                    <span>🧑‍💼</span> <span>Find Agent</span>
                </a>
                <a href="#" class="list-group-item list-group-item-action d-flex align-items-center gap-2">
                    <span>📈</span> <span>Price Charts</span>
                </a>
                <a href="#" class="list-group-item list-group-item-action d-flex align-items-center gap-2">
                    <span>🏦</span> <span>Mortgage</span>
                </a>
            </div>
        </div>


        <!-- Logo -->
        <a class="navbar-brand navbar-brand-custom" href="/">
            🏠
        </a>

        <!-- Toggle Button Mobile -->
        <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent" aria-controls="navbarContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <!-- Navbar Collapse -->
        <div class="collapse navbar-collapse" id="navbarContent">

            <!-- Search Section (Center) -->
            <div class="search-wrapper">
                <span class="search-icon">🔍</span>

                <input type="text" class="search-input" placeholder="Search...">

                <select class="location-select inside">
                    <option>Select Area</option>
                    <option>Hanoi</option>
                    <option>HCMC</option>
                    <option>Da Nang</option>
                </select>

                <button class="btn btn-search inside">Search</button>
            </div>

            

            <!-- Icons & Auth (Right) -->
            <div class="d-flex align-items-center gap-2 ms-auto">

                <!-- Heart Icon -->
                <button class="icon-btn" title="Favorites">
                    ❤️
                </button>

                <!-- Chat Icon with Badge -->
                <button class="icon-btn position-relative" title="Messages">
                    💬
                    <span class="badge-notification">5</span>
                </button>

                <!-- Notification Icon -->
                <button class="icon-btn" title="Notifications">
                    🔔
                </button>

                <!-- Post Button (Black) -->
                <button class="btn btn-action btn-post">Post Listing</button>

                <!-- Login Button (White) -->
                <a asp-controller="Auth" asp-action="Login"
                   class="btn btn-action btn-login d-none d-lg-block">
                    Sign In
                </a>
            </div>
        </div>
    </div>
</nav>

<script>
    document.addEventListener("DOMContentLoaded", function () {
        const btn = document.querySelector(".menu-toggle-circle");
        const panel = document.getElementById("categoryPanel");

        if (btn && panel) {
            btn.addEventListener("click", function () {
                const isVisible = panel.style.display === "block";
                panel.style.display = isVisible ? "none" : "block";
            });

            // Hide panel when clicking outside
            document.addEventListener("click", function (e) {
                if (!panel.contains(e.target) && !btn.contains(e.target)) {
                    panel.style.display = "none";
                }
            });
        }
    });
</script>

